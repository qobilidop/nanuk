default Order dec

$include <prelude.sail>
$include <string.sail>

// Bitvector type aliases
type bits3  = bits(3)
type bits4  = bits(4)
type bits8  = bits(8)
type bits10 = bits(10)
type bits16 = bits(16)
type bits32 = bits(32)
type bits64 = bits(64)
