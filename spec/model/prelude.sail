default Order dec

$include <prelude.sail>
$include <string.sail>

// Bitvector type aliases
type bits8  = bits(8)
type bits11 = bits(11)
type bits16 = bits(16)
type bits32 = bits(32)
type bits64 = bits(64)
