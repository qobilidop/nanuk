"""Conformance machinery shared by the test suites: the golden-model
emulator rig (harness, map_harness), scapy packet corpora (testkit), and
example loading (load). Dev-only — imports scapy, excluded from wheels."""
