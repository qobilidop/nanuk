"""Conformance machinery shared by the test suites: the golden-model
emulator rig (harness, map_harness), scapy packet corpora (testkit), example
loading (load), and the SIIT reference translator (siit_ref). Dev-only —
imports scapy, excluded from wheels."""
